package com.vhpg.popurrivault.util

/*fun Fragment.sbMessage(view: View, text: String, textColor: String? = "#FFFFFF", bgColor: String? = "#9E1734", length: Int = Snackbar.LENGTH_SHORT){
    Snackbar.make(view, text, length)
        .setTextColor(Color.parseColor(textColor))
        .setBackgroundTint(Color.parseColor(bgColor))
        .show()
}*/